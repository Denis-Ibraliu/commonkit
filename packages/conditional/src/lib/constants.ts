export const ERROR_MESSAGES = {
  SWITCH_ERROR: 'Switch component only accepts Switch.Case and Switch.Default as children.',
  CASE_ERROR:'Switch component requires at least one Switch.Case.',
  DEFAULT_ERROR:'Switch component requires exactly one Switch.Default.',
};